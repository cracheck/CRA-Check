<mah:MetroWindow x:Class="CRA_Check.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CRA_Check.Views"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        Title="About" Topmost="True"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Margin="5" Text="Version:" />
        <TextBlock Grid.Row="0" Grid.Column="1" Margin="5" Text="{Binding Version}" />
        <TextBlock Grid.Row="1" Grid.Column="0" Margin="5" Text="Syft version:" />
        <TextBlock Grid.Row="1" Grid.Column="1" Margin="5" Text="{Binding SyftVersion}" />
        <TextBlock Grid.Row="2" Grid.Column="0" Margin="5" Text="Grype version:" />
        <TextBlock Grid.Row="2" Grid.Column="1" Margin="5" Text="{Binding GrypeVersion}" />
    </Grid>
</mah:MetroWindow>
